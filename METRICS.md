# METRICS — verified against raw logs

Every number on this page was recomputed directly from
`logs/audit_log.jsonl` (302 lines) and `logs/results_checkpoint.jsonl`
(150 lines), not copied from prose in `RESULTS.md`. Where a
number matches `RESULTS.md`, that's cross-verification, not a duplicate
source. If these files are regenerated by a future run, re-derive this
page the same way — don't hand-edit numbers here.

**This page now spans two runs of the same underlying data, for two
different reasons — read this before quoting any number below.**

- **§2 (LLM proposal accuracy) still describes Run 4** (2026-08-30, after
  the third prompt-schema fix, BUILD_LOG.md §9.2) — **unchanged and still
  100% accurate**, because `llm_matched_policy` is computed in `gate.py`
  *before* either new escalation rule ever runs, so nothing about the
  compliant-escalation rules below could have touched it. Re-verified
  directly against the new log, not assumed: still 149/150 (99.3%), still
  the same single `debit_instrument_blocked` mismatch.
- **§1, §3.1, and §4 (headline/recovery/tool-call numbers) now describe
  "Run 5"** (2026-09-01) — the same 150-record dataset, re-run after
  `gate.py` gained two new compliant-escalation stopping rules
  (`MAX_ATTEMPTS_PER_SUBSCRIPTION`, `STALE_HALT_ESCALATION_DAYS` —
  BUILD_LOG.md §12). Run 4's log is preserved, not discarded, at
  `logs/pre_escalation_rules/` for direct before/after comparison,
  following this project's existing convention (`logs/pre_accuracy_fix/`,
  `logs/run3_before_third_fix/`).

**Run 5 identity:** one `run_started`, one `run_finished`,
`total_processed: 150` — single pass, no interruption, true simulate mode
(`RAZORPAY_KEY_ID=`/`RAZORPAY_KEY_SECRET=` blanked for that one process
only, `.env` itself untouched).

**Read §2.4–§2.5 before quoting any match-rate number from this page.**
The 150-record LLM-accuracy figures are really 15 unique scenarios
repeated with different amounts (`decline_description` is fixed per code,
`temperature: 0` makes output deterministic) — §2.4 tests generalization
to novel phrasing (16/16), §2.5 deliberately tries to break it with
adversarial jargon and finds one real miss, on a fraud case, with a
precise explanation of why it doesn't reach Razorpay regardless. Run 3's
own real mid-batch kill/resume story (`already_done_from_checkpoint: 124,
remaining: 26`) is covered fully in §7, not repeated here.

---

## 1. Headline numbers

| Metric | Value | Source |
|---|---|---|
| Halted subscriptions processed | **150** | `run_finished.total_processed` |
| Total value of those subscriptions | **₹150,729.35** | sum of `amount_paise` across all 150 `results_checkpoint.jsonl` records |
| Actions the gate allowed to execute | **109 / 150 (72.7%)** | `gate_executed: true` AND `final_action` in {immediate_retry, delayed_retry, payment_link_nudge} |
| Value of executed actions | **₹1,04,653.01** | sum of `amount_paise` over those 109 |
| Simulated recovered amount | **₹44,856.72** | sum of `amount_paise` where `simulated_customer_response: true` |
| Recovery rate, as % of executed actions | **40.4%** (44/109) | count where `simulated_customer_response: true` |
| Recovery rate, as % of total subscription value | **29.8%** | ₹44,856.72 / ₹1,50,729.35 |
| Hard-blocked by gate (cap/duplicate) | **0** | no `gate_execute: false` with reason citing cap or idempotency in the log |
| **Escalated by the new compliant-escalation stopping rules** | **34** (all stale-halt; 0 attempt-cap) | `gate_reason` containing "staleness threshold" / "prior recovery attempts" — see §1.1 |
| Correctly refused as fraud (never retried) | **1** | `final_action: no_action_fraud` |
| Correctly identified as unrecoverable, total | **40** | `final_action: no_action_unrecoverable` — see §1.1 for the policy-driven vs. escalated split |

**Important framing:** "recovery rate" here means *simulated* customer payment success (`decline_codes.py`'s `simulated_success_rate` per code — a labeled assumption, not a real charge). Test mode cannot produce a real completed payment. This number measures "did our simulated model of customer behavior say yes," not "did Razorpay actually collect money." Say this explicitly if asked — do not present 29.8% as a real-world recovery rate.

### 1.1 Why "unrecoverable" grew from 6 to 40, broken down honestly

Lumping these together would understate what actually happened. Of the 40
`no_action_unrecoverable` records in Run 5:

| Reason | Count | What it means |
|---|---|---|
| Genuinely policy-driven (e.g. customer cancelled, instrument permanently blocked) | **6** | Identical set to Run 4 — the decline-code policy itself says "nothing to do," unrelated to the new rules |
| Escalated: stale-halt threshold (`halted_days_ago` ≥ 12) | **34** | New in Run 5 — would have been an executed nudge/retry under Run 4's gate; now correctly handed to a human instead |
| Escalated: cross-run attempt cap (3+ prior attempts) | **0** | Expected — a fresh single-pass run has no cross-run history to trip this on; see §1.2 |

The 34 stale-halt escalations are the real, batch-scale proof this rule
does something — not just a unit test's synthetic input. A `gate_reason`
from the new log, verbatim: *"Escalated to manual review: halted 12 days
ago, at or past the 12-day staleness threshold for an automated nudge."*

### 1.2 Why the attempt-cap rule shows 0 fires here, and why that's expected, not a bug

`generate_data.py` gives every synthetic subscription a unique ID by
construction, and this was a single, fresh run against an emptied
`logs/audit_log.jsonl` — so no subscription could possibly have 3 prior
cross-run attempts yet. This is the exact same honest-disclosure shape
already established for idempotency (§6): a real mechanism that a
particular dataset/run-count doesn't happen to exercise isn't the same as
a mechanism that doesn't work. Proven instead by `tests/test_gate.py`'s
dedicated unit tests and demoable live via `python agent.py
--inject-failure repeat_attempts`. Running this same pipeline again
tomorrow, against the now-populated `logs/audit_log.jsonl`, would start
genuinely accumulating cross-run history toward this cap.

---

## 2. LLM proposal accuracy (the number the gate exists to catch)

**Definition:** "matched policy" means the LLM's raw proposed action, before the gate touches it, was *identical* to the one correct action the fixed policy table (`config/decline_policy.json`) assigns to that decline code. This is not a judgment call — every decline code maps to exactly one allowed action, so this is a strict equality check, computed directly from `llm_matched_policy` on all 150 `gate_decision` events.

| | Run 2 (schema-clarity fix) | Run 3 (+ ordered-decision-rule fix) | Run 4 (+ negative-contrast-example fix) |
|---|---|---|---|
| LLM proposal matched policy | 81 (54.0%) | 117 (78.0%) | **149 (99.3%)** |
| LLM proposal mismatched policy (gate corrected it) | 69 (46.0%) | 33 (22.0%) | **1 (0.7%)** |

The pre-fix ~87% figure BUILD_LOG.md §9.2 cites for Run 1 is asserted in
prose only — that run's raw log was not preserved, so it isn't
independently re-derivable from anything in this repo. Runs 2, 3, and 4
are all independently verified here, against `logs/pre_accuracy_fix/`,
`logs/run3_before_third_fix/`, and `logs/audit_log.jsonl` respectively.

### 2.1 Match rate by decline code — all three verified runs

Sorted by volume. Bold = 100% match. **†** = still wrong, kept here
without cherry-picking one side of the story.

| Decline code | n | Run 2 | Run 3 | Run 4 |
|---|---|---|---|---|
| insufficient_funds | 41 | 100% | 100% | **100%** |
| card_expired | 26 | 15% | **100%** | **100%** |
| authentication_failed | 17 | 35% | 6% | **100%** |
| card_declined | 11 | 9% | 0% | **100%** |
| gateway_technical_error | 8 | 50% | **100%** | **100%** |
| incorrect_cvv | 8 | 88% | **100%** | **100%** |
| card_disabled_for_online_payments | 7 | 0% | **100%** | **100%** |
| bank_technical_error | 7 | 86% | **100%** | **100%** |
| payment_timed_out | 5 | 0% | **100%** | **100%** |
| transaction_limit_exceeded | 5 | 80% | **100%** | **100%** |
| payment_failed | 5 | 20% | 0% | **100%** |
| payment_cancelled | 5 | 100% | 100% | **100%** |
| debit_instrument_inactive | 3 | 33% | **100%** | **100%** |
| payment_risk_check_failed | 1 | 100% | 100% | **100%** |
| debit_instrument_blocked | 1 | 100% | 0% | 0% † (n=1, low weight, flipped 3 times across 4 runs) |

### 2.2 What actually happened, in both directions

**The original two biases (Run 2's confusion pattern) are fully resolved.**
Every code that was previously misread as "customer-fixable card issue →
unrecoverable" or "technical/unclear decline → wait and retry" now sits at
100% match: `card_expired`, `card_disabled_for_online_payments`,
`debit_instrument_inactive`, `incorrect_cvv`, `payment_timed_out`,
`gateway_technical_error`, `bank_technical_error`,
`transaction_limit_exceeded` all moved from Run 2's confusion table
straight to 100%.

**A new, different bias appeared in Run 3, on three codes that were fine
or middling before:** `authentication_failed`, `card_declined`, and
`payment_failed` — all `bank`- or `customer`-sourced declines whose
description reads like a generic "declined by the bank" rather than an
infra outage — are now proposed as `immediate_retry` or `delayed_retry`
instead of the correct `payment_link_nudge`, every time they're wrong:

```json
{
  "authentication_failed": { "delayed_retry": 16 },
  "card_declined":         { "immediate_retry": 6, "delayed_retry": 5 },
  "payment_failed":        { "immediate_retry": 5 },
  "debit_instrument_blocked": { "no_action_fraud": 1 }
}
```

**Most likely cause** (a reasoned theory, not independently re-verified
against the model's raw reasoning text for every case): the ordered rule
added in the fix says a `bank`/`gateway`/`network`-sourced decline that's
"a system/infrastructure failure" should retry immediately — the model
appears to be pattern-matching on the *source* field alone rather than
requiring the description to actually describe downtime/timeout, so a
plain "declined by the customer's bank" gets treated the same as
"bank experienced downtime." This wasn't visible before fixing the
original two biases, since Run 2's own confusion pattern was dominated by
a different failure mode entirely — a genuine example of a fix changing
*which* failure mode dominates, not just how much failure there is.

**Net result at the time:** a large, real improvement (46%→22% override
rate) with an honestly-disclosed new failure surface on 3 of 15 codes.

### 2.3 Third fix — confirmed at full scale (Run 4)

The 3-code regression above was diagnosed precisely (re-checked against
`config/decline_policy.json` directly, not assumed): `authentication_failed`
is `decline_source: customer`, not `bank` — so it was falling through to
rule 4's "customer + funds/limit issue" bucket despite not being about
funds at all, while `card_declined`/`payment_failed` (`decline_source: bank`,
generic "declined by the bank" descriptions with no infra language) were
over-matching rule 3's "bank source → infra failure → retry immediately"
clause on source alone.

`ollama_client.py`'s tool schema was rewritten a third time with explicit
negative-contrast examples naming these exact codes (e.g. "card_declined
… → payment_link_nudge, NOT immediate_retry — bank source alone is not
enough for rule 3"). Validation happened in two stages, both real:

1. **Targeted check**, calling `propose_action()` directly on all 33
   previously-affected records (not the full 150 — a fast signal before
   committing to a ~1-hour batch run): 33/33 matched (100%).
2. **Full 150-record re-run (Run 4)**, confirming the fix at scale and
   ruling out any regression on the other 117 already-correct codes:
   **149/150 matched (99.3%)** — every one of the three previously-broken
   codes hit 100%, and nothing that was already correct broke as a side
   effect of this third rewrite. The only remaining mismatch is
   `debit_instrument_blocked`, an n=1 code that has now flipped between
   correct and incorrect three times across four runs — the signature of
   a genuinely ambiguous single case, not a systematic bias, and not
   pursued further.

**Full trajectory, all four points independently verified except Run 1:**
~87% → 46.0% → 22.0% → **0.7%** override rate.

### 2.4 An important caveat about all of the above, found and tested directly

**The "150" in every number above overstates how many distinct scenarios
were actually tested.** `decline_description` is a fixed string per code
in `config/decline_policy.json` — it never varies between records, only
the code does. Combined with `temperature: 0` (deterministic output) and
the fact that no rule references `amount_paise` or the record ID, this
means **every record sharing a decline code gets the identical proposed
action, with zero variance** — verified directly: in Run 4, `card_expired`
proposed `payment_link_nudge` in all 26 of its occurrences, `insufficient_funds`
proposed `delayed_retry` in all 41 of its occurrences, and so on for every
code, with no exceptions. **The real sample size for "does the LLM
correctly classify this kind of situation" was 15 unique scenarios, not
150** — the 150 number is only meaningful for the revenue/recovery
metrics (§1, §3.1), which do depend on how often each code occurs.

**Tested for real, not just conceded:** ran `propose_action()` against 16
hand-written alternate phrasings of the same 16 underlying situations —
deliberately sharing no wording with `config/decline_policy.json`'s fixed
descriptions (e.g. `card_expired`'s catalog text "Customer's card has
passed its expiration date" vs. the test's "The card on file expired last
month"; `gateway_technical_error`'s "Partner bank downtime prevented
payment processing" vs. "A temporary outage on the payment gateway's
servers blocked the transaction"). **Result: 16/16 (100%) matched
policy** on wording the model had never seen in this exact form. This is
real evidence the schema's rules generalize to *how a situation is
described*, not just to the 16 exact fixed strings they were tuned
against — the model appears to be reasoning about the underlying category
(customer-actionable vs. infra failure vs. funds issue vs. fraud vs.
unrecoverable), not pattern-matching literal text.

**What this does and doesn't prove, stated precisely:**
- **Does prove:** the fix isn't merely memorizing the 16 fixed catalog
  strings — paraphrased situations in a similar register (clean, concise,
  English, single-reason) are classified correctly.
- **Doesn't prove:** robustness to decline codes outside this 16-entry
  catalog, ambiguous or multi-reason real-world decline text, non-English
  text, or genuinely adversarial phrasing — §2.5 below tests exactly this,
  deliberately, and reports what actually breaks.
- **Resolved: the unknown-decline-code boundary is now explicit, not
  incidental.** `get_decline_code()` still raises `KeyError` on any code
  not in the policy table (`test_unknown_code_raises_keyerror` — that
  invariant is unchanged and still the right behavior for that function).
  But `agent.py`'s `process_one()` now checks for this *before* ever
  calling it: an unrecognized decline code is routed straight to
  `flag_for_manual_review` with its own `unknown_decline_code` audit
  event, never reaching the LLM or the gate (there's no policy for either
  to evaluate against). Previously this fell through to a generic
  per-record `try/except`, logged as an undifferentiated
  `record_processing_error` — correct in effect, but indistinguishable
  from any other kind of crash. Tested directly:
  `tests/test_agent_unknown_code.py`, 2 tests, verifying both the natural
  path and the `--inject-failure unknown_decline_code` demo path route to
  manual review and never produce a `gate_decision` event.

### 2.5 Adversarial test — deliberately trying to break it, and finding one real crack

The 16-phrasing test in §2.4 used clean, well-formed paraphrases. This one
uses real payments-industry jargon, terse log-style text, and abbreviations
a small model plausibly hasn't seen much of — `3DS`, `CNP`, `TTL`,
`acquirer`, `issuer`, `provisioned` — plus one description written to be
genuinely hard to categorize. The goal was to find where this actually
breaks, not to produce another clean number.

**Result: 15/16 (93.8%).** Fourteen jargon-heavy/terse descriptions were
classified correctly, including some that look intimidating
(`"5xx from acquirer, transient"` → `immediate_retry`, correctly; `"CNP
transactions disabled by cardholder"` → `payment_link_nudge`, correctly).

**The one failure is the highest-stakes possible miss: a fraud case.**
`payment_risk_check_failed` (correct: `no_action_fraud`), described as
*"High risk score triggered decline per issuer risk engine"*, was
proposed as `payment_link_nudge` instead — even though the description
contains the word "risk" twice. The model's own reasoning: *"the decline
description does not mention any technical/system problem"* and defaulted
to "customer must act," missing that an automated risk-engine score is
exactly what rule 1 ("bank flagged this decline specifically as
fraud/risk") is describing. This is a real, narrower-than-intended reading
of rule 1 — it seems to need something closer to "bank says this is fraud"
phrasing, not "an automated system scored this as high-risk."

**Why this doesn't threaten the actual pipeline, verified precisely, not
assumed:** `gate.evaluate()` takes `decline_code` as a direct parameter
(`gate.py` line 57) and looks up the correct policy action from
`config/decline_policy.json` using that code — **completely independent
of the LLM's proposal or its interpretation of any description text.**
In the real pipeline, `decline_code` is always the actual structured code
(from Razorpay's data or this project's synthetic data), never something
the LLM infers from free text — the description is only ever context
*for the LLM's reasoning*, not an input to the gate's decision. So if this
exact miss happened in the real pipeline, the gate would still correctly
route it to `no_action_fraud`/manual review regardless of what the LLM
proposed, exactly the same way it already corrects the 1-in-150 mismatch
in Run 4. This adversarial test is, concretely, the clearest evidence in
this entire project for why the LLM never gets to execute its own
proposal directly — found by deliberately trying to break it, not
asserted as a hypothetical.

**Bottom line on generalization:** this project has now tested three
different things, answering three different questions. The 150-record
batch runs (§2, §2.1) prove the *architecture* — proposal/execution
separation, the gate, the audit trail — works end to end at volume. The
16-phrasing test (§2.4) proves the *prompt fix* generalizes past rote
memorization, within the known 16 situation categories. This adversarial
test proves that even when the LLM *does* fail on harder, realistic
input — including on the single highest-stakes category, fraud — the
gate's decline-code-first design (not description-based) means that
failure never reaches Razorpay's API. None of this is evidence the whole
system works on a genuinely new, real-world dataset with decline codes
outside this 16-entry catalog — that would need either real Razorpay
decline data (not available) or an even larger adversarial set, which
could always surface more edge cases than three rounds of testing found.

## 3. Final action distribution (Run 5, post-escalation-rules)

| Final action | Count | % of 150 |
|---|---|---|
| payment_link_nudge | 62 | 41.3% |
| no_action_unrecoverable | 40 | 26.7% |
| delayed_retry | 33 | 22.0% |
| immediate_retry | 14 | 9.3% |
| no_action_fraud | 1 | 0.7% |

## 3.1 Recovery rate by action type

| Final action | n | Recovered | Recovery rate | Value | Recovered value |
|---|---|---|---|---|---|
| delayed_retry | 33 | 21 | **64%** | ₹27,445.51 | ₹20,770.63 |
| immediate_retry | 14 | 6 | **43%** | ₹12,856.90 | ₹6,243.05 |
| payment_link_nudge | 62 | 17 | **27%** | ₹64,350.60 | ₹17,843.04 |

(`no_action_unrecoverable` and `no_action_fraud` are excluded — by
definition nothing is attempted, so there's no recovery outcome to
measure. Per-action recovery *rates* moved somewhat from Run 4 — e.g.
`delayed_retry`'s 54%→64% — simply because the *mix* of which records
still fall into each bucket changed once 34 records moved out to
escalation; each individual record's simulated outcome is still the same
static, seeded value it always was.)

---

## 4. Tool calls

| Tool | Calls |
|---|---|
| `create_payment_link` | 62 |
| `create_retry_order` | 47 (14 immediate + 33 delayed) |
| `flag_for_manual_review` | 41 (40 unrecoverable + 1 fraud) |

150 `mcp_tool_call` events total, one per `gate_decision` — every gate
decision produced exactly one downstream tool call, no orphaned or
duplicate calls. `flag_for_manual_review` calls grew from 7 (Run 4) to 41
(Run 5) — 34 of those are the new stale-halt escalations (§1.1), not a
change in fraud/unrecoverable policy classification.

---

## 5. Gate hard-block behavior

Per `gate.py`: `MAX_ACTION_AMOUNT_PAISE` = ₹50,000/action, `MAX_RUN_TOTAL_PAISE`
= ₹5,00,000/run.

- **Subscription-recovery run:** 0 hard blocks. Every one of the 150 synthetic
  records was well under the per-action cap, and the run total (₹1,04,653.01
  executed) never approached ₹5,00,000. This means the cap has **never
  fired** in the subscription pipeline — it's implemented and unit-tested
  (`tests/test_gate.py`), but not demonstrated against real batch data. (The
  two newer *escalation* rules are a different mechanism from a hard block —
  they still `execute` the corrected/escalated action rather than refusing
  outright — and those did fire at real batch scale: 34 times, §1.1.)
- **Route demo run (`route_demo.py`), separate scenario:** 5 transfers
  attempted, **1 blocked** — `sub_route_demo_004`, reason logged verbatim
  as *"Amount 550000.00 exceeds per-action cap of 50000.00"* — an
  attempted ₹5,50,000.00 transfer correctly blocked against the
  ₹50,000.00 per-action cap. This is the only real evidence in the whole
  project of the spending cap actually intercepting an oversized action —
  worth highlighting in the pitch over the subscription pipeline's
  0-blocks number, which only proves the cap didn't get in the way, not
  that it works.

---

## 6. Idempotency

**Untested at the only scale that matters.** No `subscription_id` repeats
across the 150 synthetic records by construction (`generate_data.py`
generates unique IDs), so the idempotency check has never fired outside
`tests/test_gate.py`'s unit tests. `RESULTS.md`'s "Hard-blocked: 0" should
not be read as proof idempotency works under real conditions — it's
absence of a scenario that would have exercised it, not a passed test at
scale. This is disclosed in `BUILD_LOG.md` §12; repeated here since it
directly affects how the "0 hard-blocked" number should be interpreted.

---

## 7. Failure handling (the rubric's "one failure handled gracefully")

The current run (Run 5, `logs/audit_log.jsonl`) completed in a single
pass with **zero** `llm_parse_failure` events and no interruption. Two
real failure events remain fully demoable from preserved artifacts,
though, plus a live-triggerable option:

1. **A genuine mid-batch kill and checkpoint resume** (not staged) —
   preserved at `logs/run3_before_third_fix/`: the execution environment
   killed that run at 124/150; `agent.py` was re-invoked and resumed
   cleanly from `logs/results_checkpoint.jsonl` at the time, processing
   only the remaining 26 records with zero duplicated or lost work
   (verified: two `run_started` events, one `run_finished`, exactly 150
   total `gate_decision`/`mcp_tool_call` pairs in that snapshot). This is
   the third real instance of this exact resilience story across the
   project (BUILD_LOG.md §3.6/§12 documents two earlier ones) — this time
   captured directly in preserved artifacts, not just narrated.
2. **A real `llm_parse_failure`**, preserved at
   `logs/pre_accuracy_fix/audit_log.jsonl` (Run 2) — and reproducible live
   on demand any time via `python agent.py --inject-failure llm_parse_failure`,
   which forces this exact code path deterministically for a pitch video
   instead of relying on the model to fail on cue.
3. **`route_transfer_blocked`** (1 occurrence, Route demo, independent of
   the main pipeline) — the ₹5,50,000.00 oversized transfer (see §5) was
   blocked by the cap rather than executed.

All three are real log entries or a real, reproducible code path — not
narrated hypotheticals.

---

## 8. What these numbers do and don't prove

- **Do prove:** the gate is load-bearing regardless of how good the
  prompt gets — even at a 0.7% override rate, the gate still enforces
  spending caps and idempotency the model can never self-certify; the
  audit log is complete and internally consistent (every count here ties
  out exactly to `RESULTS.md`'s independently-written summary); the model
  has specific, nameable failure patterns rather than random noise, across
  every one of four runs (§2.2, §2.3) — including a fix's own side
  effects, which were not assumed away but found by actually re-running
  the batch, twice; the two new compliant-escalation stopping rules
  actually change real batch outcomes, not just unit-test inputs — the
  stale-halt rule fired 34/150 times in Run 5 (§1.1).
- **Don't prove:** real-world revenue recovery (everything is simulated),
  the spending cap working under real load (0 fires in 150 records),
  idempotency working under real load (0 duplicate IDs in the test set),
  the cross-run attempt-cap rule working under real load (0 fires in this
  single fresh run by construction, §1.2 — proven instead by dedicated
  unit tests), or that the LLM's accuracy holds on decline text this
  project has never seen — the 150-record match rate is really 15 unique
  scenarios repeated (§2.4), and even the 16-phrasing generalization test
  only probes paraphrases of those same 15 known situations, not new ones.
